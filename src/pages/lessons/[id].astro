---
import { getCollection, render } from "astro:content";
import Base from "../../layouts/Base.astro";
import Header from "../../components/Header.astro";
import Giscus from "../../components/Giscus.astro";

export async function getStaticPaths() {
  const lessons = await getCollection("lessons");
  return lessons.map((lesson) => ({
    params: { id: lesson.id },
    props: { lesson },
  }));
}

const { lesson } = Astro.props;
const { Content } = await render(lesson);

const date = lesson.data.created_at.toLocaleDateString("en-US", {
  year: "numeric",
  month: "long",
  day: "numeric",
});

const subtitle = lesson.data.description || "";
---

<Base title={lesson.data.title}>
  <Header currentPage="lessons" showGoBack={true} title={lesson.data.title} subtitle={subtitle} />

  <article>
    {lesson.data.link && (
      <p class="lesson-link"><span class="link-label">link:</span> <a href={lesson.data.link} target="_blank">{lesson.data.link}</a></p>
    )}

    <div class="prose">
      <Content />
    </div>
    <p class="content-date">{date}</p>
  </article>

  <Giscus category="lessons" />
</Base>

<style>
  .lesson-link {
    font-size: 0.85rem;
    color: #666;
    margin-bottom: 1.5rem;
    font-family: var(--font-mono);
  }
  .lesson-link .link-label {
    color: #999;
  }
  .lesson-link a {
    color: #666;
    text-decoration: underline;
    word-break: break-all;
  }
  .content-date {
    margin-top: 2rem;
    color: #666;
    font-size: 0.9rem;
    text-align: left;
    font-family: var(--font-mono);
  }
</style>
