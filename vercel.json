{
  "buildCommand": "rm -rf .vercel/output && git submodule update --init --recursive && bun run normalize:content-images && bun run build",
  "redirects": [
    {
      "source": "/cal",
      "destination": "https://cal.com/john.jeong/yo",
      "permanent": false
    }
  ]
}
